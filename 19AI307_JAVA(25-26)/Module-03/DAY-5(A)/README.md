# Ex.No:3(E) INNER CLASS

## QUESTION:
Write a Java program to create an inner class and access it from the outer class.

## AIM:
To write a Java program that demonstrates the use of non-static inner classes, where the inner class accesses methods to display a message based on user input.

## ALGORITHM :
1.	Start the program.
2.	Import the necessary package 'java.util'.
3.	Read the user’s name as input.
4.	Create an object of the outer class OuterClass.
5. Using the outer class object, create an object of the inner class InnerClass.
6.	Call the showMessage() method of the inner class with the name as argument.
7.Display the greeting message generated by the inner class.
8.	End the program.









## PROGRAM:
 ```
/*
Program to implement a InnerClass using Java
Developed by: VESHWANTH.
RegisterNumber:  212224230300
*/
```

## SOURCE CODE:
    import java.util.*;
    
    public class OuterClass {
    
        class InnerClass {
            void showMessage(String name) {
                System.out.println("Hello, " + name + "! This message is from the Inner Class.");
            }
        }
    
        public static void main(String[] args) {
            Scanner sc = new Scanner(System.in);
            String name = sc.nextLine();
    
            OuterClass outer = new OuterClass();
            OuterClass.InnerClass inner = outer.new InnerClass();
            inner.showMessage(name);
        }
    }


## OUTPUT:
<img width="1262" height="285" alt="image" src="https://github.com/user-attachments/assets/749597dd-b4f8-4cf6-95e7-84a3f7f71689" />





## RESULT:
The program successfully demonstrates the concept of inner classes in Java. It creates an inner class object through the outer class instance and prints a personalized greeting message.
